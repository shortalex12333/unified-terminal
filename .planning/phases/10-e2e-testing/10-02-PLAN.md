---
phase: 10-e2e-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/compatibility-matrix-validation.ts
  - scripts/verify-production-readiness.sh
autonomous: true
requirements:
  - E2E-04
  - E2E-05

must_haves:
  truths:
    - "Compatibility matrix validates 4/4 runtime adapter implementations match actual CLI behavior"
    - "Production readiness script runs 13 checks and exits 0 when all pass"
    - "Script correctly detects hardcoded paths, missing docs, build failures, and test failures"
    - "Compatibility checks cover Codex JSON output, Claude agent file, Claude session, ChatGPT DOM injection"
  artifacts:
    - path: "tests/compatibility-matrix-validation.ts"
      provides: "4 compatibility checks validating runtime adapters match real CLI behavior"
      min_lines: 80
    - path: "scripts/verify-production-readiness.sh"
      provides: "13-check production gate script with pass/fail output and exit code"
      min_lines: 80
  key_links:
    - from: "scripts/verify-production-readiness.sh"
      to: "package.json test scripts"
      via: "npm run test:unit, npm run test:integration"
      pattern: "npm run test"
    - from: "tests/compatibility-matrix-validation.ts"
      to: "src/adapters/codex/adapter.ts"
      via: "import and verify CodexAdapter capabilities"
      pattern: "import.*adapter"
    - from: "tests/compatibility-matrix-validation.ts"
      to: "src/adapters/claude/adapter.ts"
      via: "import and verify ClaudeAdapter capabilities"
      pattern: "import.*adapter"
---

<objective>
Create the compatibility matrix validation tests and production readiness verification script.

Purpose: Verify that adapter implementations match real CLI behavior (4 checks) and provide an automated 13-check production gate script that can be run before any deployment.

Output: tests/compatibility-matrix-validation.ts (4 checks) + scripts/verify-production-readiness.sh (13 checks).
</objective>

<execution_context>
@/Users/celeste7/.claude/get-shit-done/workflows/execute-plan.md
@/Users/celeste7/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-e2e-testing/10-RESEARCH.md
@docs/ONGOING_WORK/ADAPTORS/LEVEL-3-E2E-TESTING.md
@docs/ONGOING_WORK/ADAPTORS/COMPATIBILITY.md
@src/adapters/types.ts
@src/adapters/codex/adapter.ts
@src/adapters/claude/adapter.ts
@src/adapters/claude/frontmatter.ts
@src/adapters/permissions.ts
@src/adapters/factory.ts
@src/main/chatgpt-adapter.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create compatibility matrix validation tests</name>
  <files>tests/compatibility-matrix-validation.ts</files>
  <action>
Create `tests/compatibility-matrix-validation.ts` with 4 compatibility checks. Use the custom test framework pattern (test/assertEqual/assertTrue) from existing tests. Set up Electron require.cache mock before importing src/ modules.

**4 Compatibility Checks (Gemini removed -- shelved):**

1. **Codex CLI: JSON output format** -- Import CodexAdapter, create a minimal AgentConfig, verify that buildCommand() includes `--json` flag. Verify the adapter's capabilities report JSON output support. Do NOT actually spawn codex (may not be installed).

2. **Claude Code: Agent file support** -- Import ClaudeAdapter and frontmatter helpers, create a minimal AgentConfig, verify that buildCommand() produces a command that references a temp agent file path. Verify generateFrontmatter() produces valid YAML with expected fields (tools, model). Verify mapToolName() correctly translates universal tool names to Claude tool names.

3. **Claude Code: Session resume** -- Import ClaudeAdapter, create AgentConfig with sessionId set, verify that buildCommand() includes `--resume` flag with the session ID.

4. **ChatGPT Web: DOM injection capability** -- Import from chatgpt-adapter (injectText, triggerSend, isPageReady), verify the functions are exported and callable. Verify the DOM selector constants exist in dom-selectors.ts. This confirms ChatGPT Web integration exists and is wired (actual DOM testing is in E2E tests).

**Output format:**
```
=== Compatibility Matrix Validation ===

  [PASS] Codex CLI: --json flag in command
  [PASS] Claude Code: agent file with YAML frontmatter
  [PASS] Claude Code: session resume support
  [PASS] ChatGPT Web: DOM injection functions exported

Runtime       | Feature                    | Matches
--------------|----------------------------|--------
codex         | --json output              | PASS
claude        | agent file support         | PASS
claude        | session resume             | PASS
chatgpt-web   | DOM injection + Mutation   | PASS

COMPATIBILITY: 4/4 checks passed
```

At end, call process.exit with 1 if any failed, 0 if all passed.

**IMPORTANT:** No Gemini checks. Gemini is SHELVED.
  </action>
  <verify>
Run `npx ts-node tests/compatibility-matrix-validation.ts` -- must show 4 [PASS] lines, "COMPATIBILITY: 4/4 checks passed", and exit 0.
  </verify>
  <done>
4 compatibility checks pass: Codex JSON, Claude agent file, Claude session resume, ChatGPT DOM injection. No Gemini checks. Output shows formatted matrix table.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create production readiness verification script</name>
  <files>scripts/verify-production-readiness.sh</files>
  <action>
Create `scripts/verify-production-readiness.sh` -- a bash script that runs 13 production readiness checks. Each check prints [PASS] or [FAIL] with a description. Script exits 0 if all pass, 1 if any fail.

**13 Checks:**

1. **TypeScript compiles** -- Run `npx tsc --noEmit`. Pass if exit 0.
2. **Build succeeds** -- Run `npm run build:main`. Pass if exit 0.
3. **No hardcoded user paths in src/** -- Use grep to find `/Users/` in `src/ --include='*.ts'`. Pass if count is 0.
4. **No hardcoded secrets in src/** -- Use grep to find `API_KEY`, `SECRET_KEY`, `PASSWORD` in src/*.ts, excluding comments, type definitions, and process.env references. Pass if count is 0.
5. **CLI commands use safe process spawning** -- Verify files that import child_process use `execFile` or `spawn` (not `exec()`). This is a security best practice per project conventions.
6. **All adapter source files exist** -- Check `src/adapters/types.ts`, `src/adapters/codex/adapter.ts`, `src/adapters/claude/adapter.ts`, `src/adapters/claude/frontmatter.ts`, `src/adapters/permissions.ts`, `src/adapters/factory.ts` all exist.
7. **All enforcement source files exist** -- Check `src/enforcement/types.ts`, `src/enforcement/constants.ts`, `src/enforcement/enforcer.ts`, `src/enforcement/bodyguard.ts`, `src/enforcement/spine.ts`, `src/enforcement/index.ts` all exist.
8. **All skills source files exist** -- Check `src/skills/selector.ts`, `src/skills/validator.ts`, `src/skills/verify-parser.ts`, `src/skills/critical-checks.ts`, `src/skills/verify-sandbox.ts`, `src/skills/index.ts` all exist.
9. **All glue source files exist** -- Check `src/glue/assemble-prompt.ts`, `src/glue/normalizer.ts`, `src/glue/index.ts` all exist.
10. **Documentation present** -- Check these files exist: `docs/ONGOING_WORK/ADAPTORS/LEVEL-3-E2E-TESTING.md`, `docs/ONGOING_WORK/ADAPTORS/COMPATIBILITY.md`, `docs/ONGOING_WORK/ADAPTORS/LEVEL-3-TASK-SUMMARY.md`.
11. **E2E test file exists** -- Check `tests/e2e/electron-dispatch.test.ts` exists.
12. **Compatibility validation file exists** -- Check `tests/compatibility-matrix-validation.ts` exists.
13. **TypeScript strict mode enabled** -- Verify tsconfig.json contains `"strict": true`.

**Script structure:**
```bash
#!/bin/bash
# Production Readiness Verification
# Runs 13 structural/static checks. Does NOT run test suites (those are slow).
# Use npm run test:all for full runtime verification.

PASS=0
FAIL=0
TOTAL=13

check() {
  local name="$1"
  local result="$2"
  if [ "$result" -eq 0 ]; then
    echo "  [PASS] $name"
    PASS=$((PASS + 1))
  else
    echo "  [FAIL] $name"
    FAIL=$((FAIL + 1))
  fi
}

echo "=== Production Readiness Verification ==="
echo ""

# ... 13 checks using the check() helper ...

echo ""
echo "Results: $PASS passed, $FAIL failed (of $TOTAL)"
echo ""
if [ $FAIL -eq 0 ]; then
  echo "PRODUCTION READY"
  exit 0
else
  echo "NOT READY - fix $FAIL failing check(s)"
  exit 1
fi
```

After creating the script, run `chmod +x scripts/verify-production-readiness.sh`.

**IMPORTANT:** The script should work from the project root directory. Use relative paths. Do NOT reference /Users/celeste7 anywhere in the script.
**NOTE:** Checks 1 and 13 overlap intentionally -- check 1 verifies compilation, check 13 verifies strict mode is enabled in tsconfig.json.
**NOTE:** The script does NOT run unit/integration/E2E test suites (those are slow). It checks structural/static properties. The npm test scripts handle runtime verification.
  </action>
  <verify>
Run `chmod +x scripts/verify-production-readiness.sh && ./scripts/verify-production-readiness.sh` -- must show 13 [PASS] lines (or close -- some checks may fail if Plan 01 not yet complete, but the script itself must run without errors).
Run `grep '/Users/' scripts/verify-production-readiness.sh` -- must return nothing (no hardcoded paths in the script itself).
  </verify>
  <done>
Production readiness script exists at scripts/verify-production-readiness.sh, is executable, runs 13 checks with pass/fail output, exits 0 when all pass and 1 when any fail. No hardcoded paths in script.
  </done>
</task>

</tasks>

<verification>
1. `npx ts-node tests/compatibility-matrix-validation.ts` -- 4/4 checks pass
2. `./scripts/verify-production-readiness.sh` -- exits 0 (all 13 checks pass)
3. `tsc --noEmit` -- zero errors
4. No hardcoded /Users/ paths in new files
</verification>

<success_criteria>
- Compatibility matrix validates 4/4 runtime adapter implementations
- Production readiness script runs 13 structural/static checks
- Both artifacts are runnable and produce clear pass/fail output
- No Gemini references in either file
</success_criteria>

<output>
After completion, create `.planning/phases/10-e2e-testing/10-02-SUMMARY.md`
</output>
