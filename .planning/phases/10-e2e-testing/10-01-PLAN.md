---
phase: 10-e2e-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/main/index.ts
  - tests/e2e/fixtures.ts
  - tests/e2e/mocks.ts
  - tests/e2e/electron-dispatch.test.ts
autonomous: true
requirements:
  - E2E-01
  - E2E-02
  - E2E-03

must_haves:
  truths:
    - "10 E2E tests exist and exercise the full dispatch pipeline (DOM injection, rate limit, adapter dispatch, error recovery)"
    - "E2E tests use mocked DOM and mocked CLI responses for automated execution (no real ChatGPT login needed)"
    - "E2E tests follow the existing custom test framework pattern (test/assertEqual/assertTrue helpers)"
    - "Electron app supports --test-mode flag to skip ChatGPT login and disable auto-updater"
    - "npm test:e2e script exists in package.json and runs the E2E suite"
  artifacts:
    - path: "tests/e2e/electron-dispatch.test.ts"
      provides: "10 E2E tests covering DOM injection, rate limit, dispatch, error recovery"
      min_lines: 200
    - path: "tests/e2e/fixtures.ts"
      provides: "Electron app launch/teardown helpers and shared test utilities"
      min_lines: 40
    - path: "tests/e2e/mocks.ts"
      provides: "Mock ChatGPT DOM structure, mock CLI responses, error generators"
      min_lines: 60
    - path: "package.json"
      provides: "test:unit, test:integration, test:e2e npm scripts"
      contains: "test:e2e"
    - path: "src/main/index.ts"
      provides: "--test-mode flag handling"
      contains: "test-mode"
  key_links:
    - from: "tests/e2e/electron-dispatch.test.ts"
      to: "tests/e2e/fixtures.ts"
      via: "import { launchTestApp, closeTestApp }"
      pattern: "import.*fixtures"
    - from: "tests/e2e/electron-dispatch.test.ts"
      to: "tests/e2e/mocks.ts"
      via: "import mock DOM and CLI responses"
      pattern: "import.*mocks"
    - from: "tests/e2e/fixtures.ts"
      to: "playwright"
      via: "_electron.launch() for Electron lifecycle"
      pattern: "_electron.*launch"
---

<objective>
Write 10 E2E tests for the enforcement pipeline using Playwright Electron launch and mocked DOM/CLI responses, plus add npm test scripts to package.json.

Purpose: Close GAP-005 (no E2E tests for enforcement pipeline) and prove the full dispatch flow works end-to-end -- DOM injection, rate limit detection, adapter dispatch via Codex/Claude, and error recovery.

Output: tests/e2e/ directory with 3 files (10 tests), updated package.json with test scripts, updated src/main/index.ts with --test-mode flag.
</objective>

<execution_context>
@/Users/celeste7/.claude/get-shit-done/workflows/execute-plan.md
@/Users/celeste7/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-e2e-testing/10-RESEARCH.md
@docs/ONGOING_WORK/ADAPTORS/LEVEL-3-E2E-TESTING.md
@tests/claude-adapter.test.ts
@tests/integration/conductor-scheduler-executor.test.ts
@src/main/index.ts
@src/main/chatgpt-adapter.ts
@src/adapters/types.ts
@src/adapters/codex/adapter.ts
@src/adapters/claude/adapter.ts
@src/adapters/factory.ts
@package.json
@tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E test infrastructure (fixtures, mocks, npm scripts, --test-mode)</name>
  <files>
    tests/e2e/fixtures.ts
    tests/e2e/mocks.ts
    package.json
    src/main/index.ts
  </files>
  <action>
IMPORTANT: First run `npm install` in the worktree root (node_modules is empty in this worktree). Then `npm install --save-dev playwright` (core package only -- NOT @playwright/test).

1. Create `tests/e2e/fixtures.ts`:
   - Export `launchTestApp()`: uses `_electron.launch()` from `playwright` with args `['dist/main/index.js', '--test-mode']`, timeout 30_000, cwd set to project root.
   - Export `closeTestApp(app)`: calls `app.close()` with try/catch.
   - Export the custom test framework helpers: `test()`, `assertEqual()`, `assertTrue()`, `assertFalse()` -- copy exactly from `tests/claude-adapter.test.ts` pattern.
   - Export `testsPassed` and `testsFailed` counters.
   - Export `printResults(suiteName: string)`: logs summary and calls `process.exit(testsFailed > 0 ? 1 : 0)`.

2. Create `tests/e2e/mocks.ts`:
   - Export `mockChatGPTDOM()`: returns a mock DOM structure object with `inputField`, `submitButton`, `responseContainer` matching the selectors in `src/utils/dom-selectors.ts`.
   - Export `mockCLIResponse(runtime: 'codex' | 'claude')`: returns mock successful CLI output for each runtime (Codex returns JSON with `files_modified`, Claude returns structured output with `result`).
   - Export `mockRateLimitDOM()`: returns DOM content containing "You've reached your message limit" text.
   - Export `mockMalformedOutput()`: returns garbled/incomplete CLI output for error classification testing.
   - Export `mockTimeoutError()`: returns a timeout error object.
   - No Gemini mocks -- Gemini is SHELVED.

3. Update `package.json` scripts:
   - Add `"test:unit": "npx ts-node tests/task-router.test.ts && npx ts-node tests/intake.test.ts && npx ts-node tests/cli-runner.test.ts && npx ts-node tests/system-scanner.test.ts && npx ts-node tests/codex-adapter.test.ts && npx ts-node tests/fast-path.test.ts && npx ts-node tests/conductor.test.ts && npx ts-node tests/step-scheduler.test.ts && npx ts-node tests/cli-auth.test.ts && npx ts-node tests/claude-adapter.test.ts"`
   - Add `"test:integration": "npx ts-node tests/integration/conductor-scheduler-executor.test.ts"`
   - Add `"test:e2e": "npx ts-node tests/e2e/electron-dispatch.test.ts"`
   - Add `"test:compat": "npx ts-node tests/compatibility-matrix-validation.ts"`
   - Add `"test:all": "npm run test:unit && npm run test:integration && npm run test:e2e && npm run test:compat"`

4. Update `src/main/index.ts`:
   - At the top (after imports), add: `const isTestMode = process.argv.includes('--test-mode');`
   - In the `app.whenReady()` handler, wrap ChatGPT BrowserView creation in `if (!isTestMode)` to skip real ChatGPT loading in test mode.
   - In test mode, skip auto-updater initialization.
   - In test mode, skip tray icon creation.
   - Export `isTestMode` for use by other modules.
   - Do NOT remove any existing functionality -- only add conditional bypasses.

Verify `tsc --noEmit` passes after all changes.
  </action>
  <verify>
Run `tsc --noEmit` from project root -- must exit 0 with no errors.
Run `ls tests/e2e/` -- must show fixtures.ts and mocks.ts.
Run `node -e "const pkg = require('./package.json'); console.log(Object.keys(pkg.scripts).filter(k => k.startsWith('test')))"` -- must show test:unit, test:integration, test:e2e, test:compat, test:all.
  </verify>
  <done>
fixtures.ts and mocks.ts exist in tests/e2e/. package.json has 5 test scripts. src/main/index.ts has --test-mode flag. TypeScript compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write 10 E2E tests in electron-dispatch.test.ts</name>
  <files>tests/e2e/electron-dispatch.test.ts</files>
  <action>
Create `tests/e2e/electron-dispatch.test.ts` with 10 E2E tests. Import helpers from `./fixtures` and `./mocks`. Use the Electron require.cache mock pattern from `tests/integration/conductor-scheduler-executor.test.ts` (do NOT actually launch real Electron -- mock it via require.cache as the integration tests do, since Playwright launch may not work in all environments).

**Strategy:** These E2E tests verify the FULL PIPELINE by mocking Electron at the require.cache level (same pattern as Phase 8 integration tests) and exercising real module code. This is more reliable than trying to launch a real Electron app which requires display, ChatGPT login, etc.

**Test Categories and Tests:**

DOM Injection Message Flow (3 tests):
1. `E2E-01: Message injected to ChatGPT input field` -- Use mockChatGPTDOM(), call injectText() from chatgpt-adapter, verify the text property was set.
2. `E2E-02: Submit button click triggers message send` -- Call triggerSend() after injection, verify it returns success result.
3. `E2E-03: Response captured via MutationObserver` -- Set up DOM mock with response content, verify capture logic returns the response text.

Rate Limit Detection (2 tests):
4. `E2E-04: Rate limit message detected in DOM` -- Inject mockRateLimitDOM() content, verify rate limit detection returns true.
5. `E2E-05: Rate limit triggers retry logic` -- Verify the rate-limit-recovery module's deferral mechanism activates when rate limit is detected.

Full Dispatch Through Universal Adapter (3 tests):
6. `E2E-06: Codex dispatch flow` -- Create AgentConfig, call CodexAdapter.spawn(), mock child_process to return mockCLIResponse('codex'), verify the full output is parsed correctly with files_modified.
7. `E2E-07: Claude Code dispatch flow` -- Create AgentConfig, call ClaudeAdapter.spawn(), mock child_process to return mockCLIResponse('claude'), verify agent file was created and output parsed.
8. `E2E-08: Fallback chain Codex -> Claude` -- Simulate Codex spawn failure, verify system falls back to Claude adapter. Use the adapter factory's getAdapter() to verify fallback behavior.

Error Capture and Recovery (2 tests):
9. `E2E-09: CLI timeout detected and process killed` -- Spawn with very short timeout, verify process is killed and error classified as 'timeout'.
10. `E2E-10: Malformed output parsed safely with error classification` -- Feed mockMalformedOutput() through output normalization, verify error is classified (not crash).

**Structure:**
- Set up Electron mock via require.cache BEFORE importing any src/ modules (same pattern as integration test).
- Each test wrapped in the `test()` helper from fixtures.
- At end, call `printResults('E2E Dispatch Tests')`.
- File should be runnable with `npx ts-node tests/e2e/electron-dispatch.test.ts`.

**IMPORTANT:** No Gemini tests. Fallback chain is Codex -> Claude ONLY (Gemini is shelved).
**IMPORTANT:** Do NOT import from `playwright` in this file -- the require.cache mock approach is used instead of real Electron launch. The fixtures.ts file has the Playwright launch helper available for future use when real app testing is possible.
  </action>
  <verify>
Run `npx ts-node tests/e2e/electron-dispatch.test.ts` -- must show 10 [PASS] lines and exit 0.
Run `npm run test:e2e` -- must pass (uses the npm script created in Task 1).
  </verify>
  <done>
10 E2E tests pass covering DOM injection (3), rate limit (2), dispatch (3), error recovery (2). All tests use mocked DOM/CLI. No Gemini tests. GAP-005 is closeable.
  </done>
</task>

</tasks>

<verification>
1. `npm run test:e2e` passes -- 10/10 E2E tests
2. `tsc --noEmit` -- zero errors
3. `npm run test:unit` still passes -- no regressions to existing 444+ tests
4. `npm run test:integration` still passes -- 23/23
5. `grep 'test-mode' src/main/index.ts` -- flag exists
6. `ls tests/e2e/` -- 3 files present
</verification>

<success_criteria>
- 10 E2E tests pass covering the 4 categories: DOM injection, rate limit, dispatch, error recovery
- No test regressions in existing suites
- npm test scripts work for all categories
- --test-mode flag added to Electron app
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/10-e2e-testing/10-01-SUMMARY.md`
</output>
