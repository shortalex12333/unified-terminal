7 Domain Specs — Each lists actors involved, what we took from which plugin (exact prompt names, what we changed, why), what we deleted, hard rails, soft rails, and file paths.
HARD-RAILS.md — All 11 code-based checks with complete Python/bash implementations. No AI anywhere. Each check has: what it does, why it exists, where it activates, which tier triggers it, the actual script, confidence classification (definitive vs heuristic), and false positive handling. The Bodyguard dispatcher implementation is included in TypeScript.
AGENT-TOPOLOGY-MVP.md — Every agent classified into three tiers (infrastructure/code, orchestration/LLM long-lived, workers/LLM short-lived). Each agent has: prompt source, typical runtime, duration, and sub-agent potential. The routing flow from user message through Tier 0/1/2/3 to worker spawn is mapped. Sub-agent spawning rules are defined by task size (1-2 files = direct, 3-5 = may sub-agent, 6+ = should sub-agent). MVP build order is phased: Day 1 minimum (4 agents), Week 1 expansion (4 more), Week 2 full (5 more).
ADAPTERS.md — All four runtime adapters dissected: Codex CLI (~250 lines), Claude Code (~200), Gemini CLI (~200), ChatGPT Web (~300). Each has: how it works, spawn command construction, output parsing, session management, error codes, model routing, and exactly what is missing for our implementation. The AgentConfig interface and AgentResult interface define the contracts everything else programs against.