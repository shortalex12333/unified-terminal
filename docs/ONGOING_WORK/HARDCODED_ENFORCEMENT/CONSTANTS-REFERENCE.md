819 lines, 24 sections, every value is a constant that goes into code. Here's what's in it:
Section 1-3 are the heaviest: Context Warden token thresholds per model (7 models, exact kill percentages and effective token counts), all 7 cron intervals with millisecond values, and every timeout in the system from agent spawn (10s) to auth flow (5min) to kill grace period (5s).
Section 4-6 cover circuit breaker rules (3 retries then ask user, definitive checks get NO skip button), file size thresholds (50 bytes minimum for non-stub, 1KB minimum for screenshots), and tier classification with exact agent lists per tier.
Section 7-9 cover project state machine timers (15min→PAUSED, 24h→CLOSED, automatic archivist trigger), sub-agent spawning rules (1-2 files direct, 3-5 may split, 6+ must split, max 3 parallel), and retry policies for all 11 enforcement checks.
Section 10-11 are the complete hard rail catalog — every check with its exact command, pass condition, false positive handling, secondary check, and the activation map showing which checks fire on which step types, all dispatched in parallel via Promise.allSettled.
Sections 12-24 cover token budget allocation per phase, model routing per runtime, tool-to-sandbox translation (the corrected Codex mapping), deploy health checks, rate limit regex patterns, DOM polling intervals, latency budgets, skill injector thresholds, lesson validation regex, scope enforcement whitelists, responsive viewport sizes, intake constraints, and memory limits.
The final table explicitly separates what's code-enforced (the 11 hard rails catching ~80% of failures) from what's still LLM judgment (PA comparison, code review, security review, design compliance — the remaining ~20%). Every soft rail is named so nobody mistakes it for a hard guarantee.