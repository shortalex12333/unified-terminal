{
  "skill": "token-threshold-enforcement",
  "description": "Monitor API token usage against model-specific thresholds",
  "checks": [
    {
      "id": 10,
      "name": "token-threshold",
      "label": "Token Usage Threshold",
      "description": "Check cumulative API token usage is below model-specific threshold to prevent runaway costs",
      "script": "checks/check_tokens.py",
      "pass": "token_usage < TOKEN_THRESHOLDS[model].killAt percentage",
      "confidence": "heuristic",
      "when": "during step execution (every 30 seconds)",
      "retries": {
        "attempts": 1,
        "delayMs": 0
      },
      "timeout": 30000,
      "optional": false,
      "tier": 2,
      "phase": "during-step"
    }
  ],
  "activation": {
    "triggers": ["during_step_execution"],
    "tiers": [1, 2, 3],
    "skipConditions": [
      "no API token configured"
    ]
  },
  "failure": {
    "severity": "warning",
    "userMessage": "Token usage approaching model threshold. Current usage shown in monitoring. Agent may be killed if threshold exceeded (unless task is >85% complete).",
    "allowSkip": true,
    "options": ["Continue", "Reduce output", "Stop build"]
  }
}
